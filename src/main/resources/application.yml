spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/crm1911?useUnicode=true&characterEncoding=UTF-8
    driver-class-name: com.mysql.jdbc.Driver
    
mybatis:
  mapper-locations:
  - classpath:mapper/*.xml
  
  
server:  port: 80
  
  
logging:
  level:
    com.st.dao: debug